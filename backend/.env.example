# Backend environment variables
# Controls which origin is allowed by CORS in music_backend settings
FRONTEND_URL=http://localhost:3000

# Optional Django settings
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=dev-secret-not-for-prod
